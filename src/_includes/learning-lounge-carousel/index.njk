{% carousel 'learning-lounge-carousel', 3 %}
  {%- for event in collections.learningLounge -%}
    {% if (loop.index <= 6) %}
      {% carouselSlide %}
        {% set uri %}
          {% eventUri event %}
        {% endset %}

        {% card event.data.title, event.data.prettyType, uri %}
          <div>
            <span class="{% className cardCss, 'footer' %}">
              DAY {% dateToSummitDay event.data.start %}, {% confDate event.data.start, 'hh:mm' %} – {% confDate event.data.end, 'hh:mm' %}
            </span>
          </div>
        {% endcard %}
      {% endcarouselSlide %}
    {% endif %}
  {%- endfor -%}
{% endcarousel %}
